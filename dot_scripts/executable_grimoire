#!/usr/bin/env -S just --working-directory . --justfile
set quiet
set shell := ["bash", "-uc"]

[private]
default:
  echo "Personal project manager, with some witchcraft ✨"; echo
  just --justfile {{justfile()}} --list


# List the projects
list:
  gh repo list --source --no-archived
